포트
web : 80
ssh : 22
포트번호를 바꿀 수 있다.

포트 포워딩
라우터에도 포트가 있고 서버를 설치한 나의 컴퓨터에도 포트가 있다.
누군가가 라우터의 특정포트로 들어오게 되면 나의 컴퓨터로 연결이 된다.

내부에서 사용되는 ip : default gateway
ip route : 에서 default라 쓰여있는 것이 default gateway 
위의 아이피로 이동시 라우터 설정하는 곳으로 연결 됨
이곳에서 라우터 설정을 할 수 있다.
